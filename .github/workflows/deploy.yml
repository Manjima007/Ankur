name: Frontend Vercel Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy-frontend:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Webhook
        run: curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK_ }}"